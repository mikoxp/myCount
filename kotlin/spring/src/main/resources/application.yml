spring:
  datasource:
    url: jdbc:h2:file:./db
    driverClassName: org.h2.Driver
    username: db
    password: db
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true
    hibernate:
      ddl-auto: validate
  h2:
    console:
      enabled: true